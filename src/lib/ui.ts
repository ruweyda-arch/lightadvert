/**
 * A plain date/text input styled to match `NativeSelect` and the shadcn Input,
 * for the compact GET filter bars on server-rendered list pages. Full width on
 * mobile, auto width from `sm:` up.
 */
export const filterFieldClass =
  "border-input h-9 w-full rounded-md border bg-transparent px-2 text-sm outline-none " +
  "focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] sm:w-44";
